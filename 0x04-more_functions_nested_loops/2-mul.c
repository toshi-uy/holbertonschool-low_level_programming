#include "holberton.h"

/**
 * mul - checks for alpha character
 * @x: parameter x
 * @y: parameter y
 * Description: checks if parameter (int c) is a lowercase character
 *
 * Return: Always 0.
 */

int mul(int x, int y)
{
	return (x * y);
}
