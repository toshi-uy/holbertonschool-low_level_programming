#include <stdlib.h>
#include <stdio.h>
/**
 *main - Entry point
 *
 *Description: Positive anything is better than negative nothing
 *
 *Return: Always 0 (Success)
 */

int main(void)
{
	char az;

	for (az = 'a' ; az <= 'z' ; az++)
	{
		if (az != q && az != e)
			putchar(az);
	}
	putchar('\n');
	return (0);
}
